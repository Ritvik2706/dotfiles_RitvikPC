# PATH — consolidated, no duplicates
export PATH="$HOME/.local/bin:$HOME/bin:$HOME/.npm-global/bin:$PATH"

# pyenv root (shims + versions; binary may be system-installed)
export PYENV_ROOT="$HOME/.pyenv"

# API keys — uncomment and fill as needed
export TAVILY_API_KEY="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"
# export GEMINI_API_KEY=""
# export GROQ_API_KEY=""
